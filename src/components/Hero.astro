---
import { motion } from 'framer-motion';
---

<section class="relative overflow-hidden min-h-screen flex items-center bg-gradient-to-br from-base-100 via-base-100 to-base-200 hero-section">
  <!-- Éléments de background sophistiqués -->
  <div class="absolute inset-0 -z-10">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-primary/10 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob"></div>
    <div class="absolute top-1/3 right-1/4 w-96 h-96 bg-secondary/10 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob animation-delay-2000"></div>
    <div class="absolute bottom-1/4 left-1/2 w-96 h-96 bg-accent/10 rounded-full mix-blend-multiply filter blur-3xl opacity-70 animate-blob animation-delay-4000"></div>
    
    <!-- Lignes de grille subtiles -->
    <div class="absolute inset-0 opacity-5 bg-[linear-gradient(90deg,#000_1px,transparent_1px)] bg-[size:60px_60px]"></div>
    <div class="absolute inset-0 opacity-5 bg-[linear-gradient(0deg,#000_1px,transparent_1px)] bg-[size:60px_60px]"></div>
  </div>

  <div class="container mx-auto px-4 py-16 md:py-24">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
      <!-- Contenu texte -->
      <div class="relative">
        <!-- Badge élégant -->
        <div class="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-gradient-to-r from-primary/10 to-secondary/10 border border-primary/20 backdrop-blur-sm mb-10">
          <div class="flex items-center gap-2">
            <div class="w-2 h-2 bg-primary rounded-full animate-pulse"></div>
            <span class="text-sm font-semibold tracking-wider">AGENCE CRÉATIVE DIGITALE</span>
          </div>
        </div>

        <!-- Titre principal avec vos textes -->
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold mb-8 leading-tight title-animate">
          <span class="block text-base-content">Créations Digitales</span>
          <span class="block text-gradient mt-4">Qui Captivent</span>
          <span class="block text-primary mt-4">Vos Clients</span>
        </h1>

        <!-- Sous-titre avec votre texte -->
        <p class="text-xl md:text-2xl opacity-90 mb-12 max-w-2xl leading-relaxed text-reveal">
          BKS Concept crée des expériences digitales uniques qui captivent vos clients et boostent votre business.
        </p>

        <!-- CTA Buttons - vos textes -->
        <div class="flex flex-col sm:flex-row gap-6 mb-16">
          <a 
            href="/contact" 
            class="btn btn-gradient btn-lg rounded-xl px-10 text-lg font-semibold shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300 group relative overflow-hidden btn-wave"
          >
            <span class="relative z-10 flex items-center gap-3">
              <span>Commencer mon projet</span>
              <span class="group-hover:translate-x-2 transition-transform duration-300">→</span>
            </span>
          </a>
          
          <a 
            href="/portfolio" 
            class="btn btn-outline btn-lg rounded-xl px-10 text-lg font-semibold border-2 hover:border-primary hover:bg-primary/5 group transition-all duration-300 btn-wave"
          >
            <span class="flex items-center gap-3">
              <span>Voir nos réalisations</span>
              <span class="opacity-0 group-hover:opacity-100 transition-opacity duration-300">✨</span>
            </span>
          </a>
        </div>

        <!-- Statistiques professionnelles -->
        <div class="flex flex-wrap gap-10 pt-10 border-t border-base-300/50">
          <div class="text-center">
            <div class="text-3xl font-bold text-primary counter-effect" data-target="50">0</div>
            <div class="text-sm opacity-80 tracking-wide">PROJETS RÉALISÉS</div>
          </div>
          <div class="text-center">
           <div class="text-3xl font-bold text-secondary counter-effect" data-target="98">0</div>
            <div class="text-sm opacity-80 tracking-wide">SATISFACTION CLIENT</div>
          </div>
          <div class="text-center">
            <div class="text-3xl font-bold text-accent counter-effect" data-target="24">0</div>
            <div class="text-sm opacity-80 tracking-wide">SUPPORT TECHNIQUE</div>
          </div>
        </div>
      </div>

      <!-- Visualisation professionnelle avec vos textes -->
      <div class="relative">
        <!-- Carte principale redesignée -->
        <div class="relative z-10 bg-base-100 rounded-2xl shadow-2xl border border-base-300 overflow-hidden transform hover:-translate-y-2 transition-transform duration-500 floating-element">
          <!-- Header du navigateur -->
          <div class="p-5 border-b border-base-300 bg-gradient-to-r from-base-200 to-base-100">
            <div class="flex items-center gap-3">
              <div class="flex gap-1.5">
                <div class="w-3 h-3 rounded-full bg-red-400/80"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-400/80"></div>
                <div class="w-3 h-3 rounded-full bg-green-400/80"></div>
              </div>
              <div class="flex-1">
                <div class="bg-base-300 rounded-lg px-4 py-2.5">
                  <div class="text-sm font-mono text-base-content/70 text-center">https://votre-business.com</div>
                </div>
              </div>
              <div class="flex gap-1.5 opacity-0">
                <div class="w-3 h-3 rounded-full bg-transparent"></div>
              </div>
            </div>
          </div>
          
          <!-- Contenu de la carte avec vos textes -->
          <div class="p-10">
            <div class="flex flex-col items-center text-center">
              <!-- Icône moderne -->
              <div class="relative mb-8">
                <div class="absolute inset-0 bg-gradient-to-r from-primary to-secondary blur-xl opacity-30 rounded-full"></div>
                <div class="relative w-24 h-24 rounded-2xl bg-gradient-to-br from-primary to-secondary flex items-center justify-center shadow-lg">
                  <span class="text-4xl text-white">🚀</span>
                </div>
              </div>
              
              <!-- Vos titres -->
              <h3 class="text-2xl md:text-3xl font-bold mb-4">Site Web Professionnel</h3>
              <p class="text-lg opacity-80 mb-8 max-w-md">Optimisé pour la Conversion Client</p>
              
              <!-- Indicateurs de performance -->
              <div class="w-full max-w-sm space-y-6">
                <div>
                  <div class="flex justify-between items-center mb-2">
                    <span class="font-medium text-sm">Performance SEO</span>
                    <span class="text-sm font-bold text-primary">95%</span>
                  </div>
                  <div class="h-2 bg-base-300 rounded-full overflow-hidden">
                    <div class="h-full bg-gradient-to-r from-primary to-secondary w-11/12"></div>
                  </div>
                </div>
                
                <div>
                  <div class="flex justify-between items-center mb-2">
                    <span class="font-medium text-sm">Taux de Conversion</span>
                    <span class="text-sm font-bold text-secondary">+42%</span>
                  </div>
                  <div class="h-2 bg-base-300 rounded-full overflow-hidden">
                    <div class="h-full bg-gradient-to-r from-secondary to-accent w-4/5"></div>
                  </div>
                </div>
                
                <div>
                  <div class="flex justify-between items-center mb-2">
                    <span class="font-medium text-sm">Satisfaction Client</span>
                    <span class="text-sm font-bold text-accent">98%</span>
                  </div>
                  <div class="h-2 bg-base-300 rounded-full overflow-hidden">
                    <div class="h-full bg-gradient-to-r from-accent to-primary w-full"></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Éléments décoratifs flottants -->
        <div class="absolute -top-8 -right-8 w-28 h-28 bg-gradient-to-br from-primary/20 to-transparent rounded-2xl rotate-12 border border-primary/10 backdrop-blur-sm floating-element animation-delay-1000"></div>
        <div class="absolute -bottom-10 -left-8 w-36 h-36 bg-gradient-to-tr from-secondary/20 to-transparent rounded-3xl -rotate-12 border border-secondary/10 backdrop-blur-sm floating-element animation-delay-2000"></div>
        
        <!-- Badge certification -->
        <div class="absolute -bottom-5 right-12 bg-base-100 border border-base-300 rounded-xl px-5 py-3 shadow-lg backdrop-blur-sm">
          <div class="flex items-center gap-3">
            <div class="w-2.5 h-2.5 bg-green-400 rounded-full animate-pulse"></div>
            <span class="text-sm font-semibold">Certifié Performance</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator élégant -->
  <div class="absolute bottom-10 left-1/2 transform -translate-x-1/2">
    <div class="animate-bounce">
      <div class="flex flex-col items-center">
        <span class="text-xs opacity-60 mb-2 tracking-wider">EXPLORER</span>
        <div class="w-5 h-9 border-2 border-base-300/50 rounded-full flex justify-center">
          <div class="w-1 h-2 bg-base-300/50 rounded-full mt-2 animate-pulse"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .text-gradient {
    background: linear-gradient(90deg, #3B82F6, #8B5CF6, #EC4899);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .btn-gradient {
    background: linear-gradient(90deg, #3B82F6, #8B5CF6);
    color: white;
    border: none;
    position: relative;
    overflow: hidden;
  }

  .btn-gradient::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.7s;
  }

  .btn-gradient:hover::before {
    left: 100%;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0) rotate(var(--rotation, 0deg)); }
    50% { transform: translateY(-20px) rotate(var(--rotation, 0deg)); }
  }

  .floating-element {
    animation: float 8s ease-in-out infinite;
    --rotation: 0deg;
  }

  .floating-element:nth-child(2) {
    --rotation: 12deg;
    animation-delay: 1s;
  }

  .floating-element:nth-child(3) {
    --rotation: -12deg;
    animation-delay: 2s;
  }
  
  @keyframes blob {
    0% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
    100% { transform: translate(0px, 0px) scale(1); }
  }
  
  .animate-blob {
    animation: blob 7s infinite;
  }
  
  .animation-delay-1000 {
    animation-delay: 1s;
  }
  
  .animation-delay-2000 {
    animation-delay: 2s;
  }
  
  .animation-delay-4000 {
    animation-delay: 4s;
  }
</style>
